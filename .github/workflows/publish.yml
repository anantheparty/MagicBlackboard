name: publish (disabled during Foundation)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    # The inherited Dockerfile packages apps/web, not Magic Blackboard. Keep
    # this workflow intentionally inert until a reviewed product image,
    # protected environment, and release gate replace it.
    runs-on: ubuntu-latest
    steps:
      - name: Explain release gate
        run: |
          echo "Publishing is disabled during the Foundation milestone."
          echo "No credentials are read and no artifact is produced."
